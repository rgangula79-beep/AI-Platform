import {useState} from "react";import {useAuth} from "../context/AuthContext";import api from "../services/api";
export default function Profile(){const{user}=useAuth();const[name,setName]=useState(user?.name||"");const[msg,setMsg]=useState("");async function save(){try{const{data}=await api.put("/user/profile",{name});setMsg(`Saved as ${data.user.name}`)}catch(e){setMsg(e.response?.data?.message||e.message)}}return <div className="page"><h1>Profile</h1><div className="card"><label>Name<input value={name} onChange={e=>setName(e.target.value)}/></label><label>Email<input value={user?.email||"Guest"} disabled/></label><p>Plan: <strong>{user?.plan}</strong></p><button className="primary" onClick={save}>Save</button>{msg&&<div className="notice">{msg}</div>}</div></div>}
